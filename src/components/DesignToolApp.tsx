import App from '../App';

/**
 * Wrapper component for the React design tool.
 * Used as an Astro island with client:only directive.
 */
export default function DesignToolApp() {
  return <App />;
}
